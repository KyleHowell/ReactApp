import {createAction} from 'redux-actions';

export const setBackgroundColor = createAction('SET_BACKGROUND_COLOR');

export const setCheck = createAction('SET_CHECK');

export default {setBackgroundColor, setCheck};
